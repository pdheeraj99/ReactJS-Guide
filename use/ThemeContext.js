import { createContext } from 'react';

/**
 * Idi manam `use` hook tho read cheyabotunna simple context.
 * Default value 'light' ga set chesam.
 */
export const ThemeContext = createContext('light');